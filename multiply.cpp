#include <iostream>
using namespace std;
int main()
{
    int a, b;

    cout << "---------------------------------" << endl;
    cout << "Enter the first number : " << endl;
    cin >> a;

    cout << "Enter the second number : " << endl;
    cin >> b;

    int product = (a * b);
    cout << "The product is : " << product << endl;
    return 0;
}